"""Posting where the world can see it.

One module per platform, behind one shape, for the same reason the model
providers are: LinkedIn will change its API, and when it does one file
should change rather than everything that ever posts.
"""
